tasks:
  - name: Pinging myself at "{{ machine.Hostname }}"
    action: ping -c 3 {{ machine.Hostname }}

  - name: Get uname
    action: uname -a

  - name: Echo "{{ vars.service }}" 
    action: echo "{{ vars.service }} at {{ machine.Hostname}}" 
