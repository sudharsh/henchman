vars:
  service: iptables
  
hosts:
  - 192.168.66.11
  - 192.168.66.12
  
tasks:
  - name: Get uname
    action: uname -a

  - name: Checking if package {{ .service }} exists
    action: sudo /sbin/service {{ .service }} status

